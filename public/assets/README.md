# public/assets

This folder contains static assets for the project. Files placed here are served at `/assets/<filename>`.

- images/ — image assets (PNG, JPG, WebP)
- icons/ — small icon SVGs

Examples:
- /assets/logo.svg
- /assets/images/example.png

Add your assets here and reference them from the app or CSS.