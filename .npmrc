# Ensure consistent package installation across platforms
engine-strict=false
save-exact=false
package-lock=true

# Use LF line endings for scripts
script-shell=/bin/sh

# Include optional dependencies (needed for lightningcss native bindings)
optional=true

